# Local development overrides for docker-compose.yml
# Copy this file to docker-compose.override.yml when running locally on HTTP (localhost)
services:
  client:
    volumes:
      - ./client/nginx.local.conf:/etc/nginx/conf.d/default.conf:ro
